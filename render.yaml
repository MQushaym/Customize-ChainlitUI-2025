services:
  - type: web
    name: grc-llm-chainlit
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: chainlit run ui.py --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
        
